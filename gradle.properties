# Project flags
demoAppMaplibreAndroidFlavor=default
androidMinSdk=23
androidCompileSdk=36
androidTargetSdk=36
iosDeploymentTarget=12.0
# Compiler JDK for every JVM compilation. Must be >= desktopJvmTarget.
jvmToolchain=25
# Android bytecode target. Independent of the desktop target below.
androidJvmTarget=11
# Desktop bytecode target. MapLibre Native FFI ships Java 24 bytecode and uses
# the FFM API, so desktop cannot go below 24; the rewrite targets 25.
desktopJvmTarget=25
maplibreIosVersion=6.25.1
#
# Dependency flags
kotlin.code.style=official
kotlin.daemon.jvmargs=-Xmx2048M
kotlin.incremental.wasm=true
kotlin.mpp.enableCInteropCommonization=true
kotlin.native.ignoreDisabledTargets=true
kotlin.native.enableKlibsCrossCompilation=false
org.jetbrains.compose.experimental.jscanvas.enabled=true
org.gradle.configuration-cache=false
# 2048M was not enough to link the iOS release frameworks: both
# linkReleaseFrameworkIosArm64 and its simulator sibling died with an
# OutOfMemoryError, so `./gradlew build` could not complete at all. Release
# linking runs the whole Kotlin/Native optimizer over one binary, which is the
# heaviest thing this build does -- and it runs in the Gradle daemon, so this is
# the setting that governs it rather than kotlin.daemon.jvmargs, which was
# measured and made no difference.
org.gradle.jvmargs=-Xmx8g -Dfile.encoding=UTF-8
android.nonTransitiveRClass=true
# TODO: Split the Android app from the KMP demo module, then remove these AGP 9 opt-outs.
# https://kotl.in/kmp-project-structure-migration
android.builtInKotlin=false
android.newDsl=false
android.useAndroidX=true
mavenCentralPublishing=true
mavenCentralAutomaticPublishing=true
signAllPublications=true
spmforkmp.disableStartupFile=true
POM_URL=https://github.com/maplibre/maplibre-compose/
POM_LICENSE_NAME=BSD-3-Clause
POM_LICENSE_URL=https://opensource.org/license/bsd-3-clause
POM_LICENSE_DIST=repo
POM_SCM_URL=https://github.com/maplibre/maplibre-compose
POM_SCM_CONNECTION=scm:git:git://github.com/maplibre/maplibre-compose.git
POM_SCM_DEV_CONNECTION=scm:git:ssh://git@github.com/maplibre/maplibre-compose.git
POM_DEVELOPER_ID=maplibre
POM_DEVELOPER_NAME=MapLibre
POM_DEVELOPER_URL=https://github.com/maplibre/
