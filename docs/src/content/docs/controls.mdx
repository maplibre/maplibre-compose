---
title: Overlay Compose UI
description: Position the logo, attribution, and your own controls on top of the map.
---

import { Aside, Code } from "@astrojs/starlight/components";
import Api from "../../components/Api.astro";
import { region } from "../../snippets";

import controls from "../../../../demo-app/common/src/commonMain/kotlin/org/maplibre/compose/docsnippets/Controls.kt?raw";
import material3 from "../../../../demo-app/common/src/commonMain/kotlin/org/maplibre/compose/docsnippets/Material3.kt?raw";

<Api of="MaplibreMap" /> draws controls on top of the map. The `overlay`
block replaces the default controls when you supply it. The default draws a
scale bar and a compass along the top edge. It draws the MapLibre logo and an
attribution button along the bottom edge. The scale bar and the compass appear
only while they are relevant.

<Code code={region(controls, "default")} lang="kotlin" title="App.kt" />

<Aside type="caution">
  Most tile sources require attribution. If you replace `MapOverlay.Default`,
  show the source attribution yourself.
</Aside>

Two variants cover common needs. `MapOverlay.Full` adds zoom in and zoom out
buttons at the middle of the end edge. `MapOverlay.AttributionOnly` draws only
the logo and the attribution button.

## Replace the overlay

An empty block, or `MapOverlay.None`, draws no controls. Use it when your app
shows the attribution somewhere else, such as an about screen.

<Code code={region(controls, "disabled")} lang="kotlin" title="App.kt" />

The trailing block draws the controls that you list. Inside `Controls`, `Modifier.align` positions
a control against an inset edge of the map.

<Code code={region(controls, "custom")} lang="kotlin" title="App.kt" />

## Theme the controls with Material 3

The base library draws controls with a fixed palette that stays legible on any
basemap. The `maplibre-compose-material3` module draws the same controls with
your Material 3 color scheme and typography. Add the dependency:

```toml title="libs.versions.toml"
[libraries]
maplibre-composeMaterial3 = { module = "org.maplibre.compose:maplibre-compose-material3", version = "{{release}}" }
```

```kotlin title="build.gradle.kts"
commonMain.dependencies {
  implementation(libs.maplibre.composeMaterial3)
}
```

<Api of="Material3" display="MapOverlay.Material3" /> draws the same controls
as <Api of="MapOverlay.Default" /> in the same places. Include it in the block:

<Code code={region(material3, "overlay")} lang="kotlin" title="App.kt" />

`MapOverlay.Material3AttributionOnly` and `MapOverlay.Material3Full` apply the
theme to the other presets.

Each Material 3 control is also a composable that you can place in the trailing
block:

<Code code={region(material3, "controls")} lang="kotlin" title="App.kt" />

1. `ScaleBar` and `CompassButton` stay on screen. The `Disappearing` versions
   that `MapOverlay.Material3` uses fade in when the zoom or the orientation
   changes, and fade out once it settles.

## Inset overlays and the camera

The overlay is a full-map `Box`: ordinary sizing and alignment apply no implicit
padding. Use <Api of="Controls" /> for controls. It uses the larger
of `cameraPadding` and safe-drawing insets on each edge, then adds
<Api of="MapOverlay.Spacing" />. The built-in presets use this layout.

`cameraPadding` shifts the camera center and supplies the default control insets.
Override `Controls(contentPadding = ...)` when controls need different padding.
`contentWindowInsets` on `Controls` defaults to `WindowInsets.safeDrawing`; override
it to choose different system insets. Padding and insets are consumed using Compose
layout rules, so an outer layout can already have handled the safe area.
Insets follow the supplied padding immediately, without waiting for a map frame.

<Code code={region(controls, "insets")} lang="kotlin" title="App.kt" />

1. `union` takes the larger inset on each edge.

Padding on a bounding-box camera move adds to `cameraPadding` for that fit.

## Pin Compose UI to a location

<Api of="AtPosition" /> puts a child on a geographic position.
Include <Api of="MapOverlay.Default" /> to keep the default controls. A pan, a
zoom, or a window resize moves the child with the map.

<Code code={region(controls, "placedAt")} lang="kotlin" title="App.kt" />

1. `Alignment.BottomCenter` sits the bottom edge of the chip on the point.
   Padding lifts the chip above the feature.

## Point at an off-screen location

Use <Api of="TowardsPosition" /> for a direction indicator when
a location is out of view. It places the child along an ellipse inside its
layout bounds and hides it while the location is inside that ellipse. Nest it in
`Controls` to use the unobstructed region, or size and pad it with normal Compose
layouts. Geographic placements convert map coordinates into their local layout,
including when nested or offset.

<Code code={region(controls, "placedTowards")} lang="kotlin" title="App.kt" />

1. The state stores the placement that the overlay computed.
2. `angleDegrees` is the direction of the target, clockwise from screen-up.

<Api of="PointerPinButton" /> from the Material 3 module uses this layout
to display a pin-shaped button that points at the target.
