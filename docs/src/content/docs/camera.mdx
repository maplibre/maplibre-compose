---
title: Control the camera
description: Set the initial position, animate the camera, fit bounds, and convert coordinates.
---

import { Code } from "@astrojs/starlight/components";
import Api from "../../components/Api.astro";
import { region } from "../../snippets";

import camera from "../../../../demo-app/common/src/commonMain/kotlin/org/maplibre/compose/docsnippets/Camera.kt?raw";

The camera defines the visible part of the map: a target position, a zoom
level, a bearing, and a tilt. <Api of="rememberMapState" /> creates a
<Api of="MapState" /> with a read-only camera position. Pass an
initial <Api of="CameraPosition" /> to set the camera at startup:

<Code code={region(camera, "first-position")} lang="kotlin" title="App.kt" />

## Animate the camera

<Api of="MapState.animateCameraPosition" /> is a suspend function that moves
the map to a new position over a duration. Call it from a coroutine. The
function waits until the map is attached:

<Code code={region(camera, "animate")} lang="kotlin" title="App.kt" />

Use <Api of="MapState.setCameraPosition" /> to move without an animation. The
position is retained when the map is detached and restored when it is displayed
again.

## Fit a bounding box

<Api of="MapState.animateCameraToBounds" /> fits a `BoundingBox` in the current
viewport. Padding adds space between the box and the map edges:

<Code code={region(camera, "fit-bounds")} lang="kotlin" title="App.kt" />

Use <Api of="MapState.fitCameraToBounds" /> to fit the same bounding box without
an animation.

Use <Api of="MapState.cameraForBounds" /> to calculate a position before applying
it. The query waits for a viewport and leaves the current camera and animation
unchanged. For example, cap the calculated zoom before animating:

<Code code={region(camera, "camera-for-bounds")} lang="kotlin" title="App.kt" />

On the browser, bounds fitting calculates the target and zoom without tilt,
then assigns the requested tilt. With nonzero tilt, some bounds may fall outside
the viewport.

A bounding box wastes space around a diagonal route or a scattered selection
when the camera is rotated. <Api of="MapState.cameraForGeometry" /> and
<Api of="MapState.cameraForCoordinates" /> fit the positions themselves, so a
rotated camera stays as close as the positions allow:

<Code code={region(camera, "camera-for-geometry")} lang="kotlin" title="App.kt" />

Positions are used as given. Write a route that crosses the antimeridian with
continuous longitudes, such as 179 followed by 181. Empty geometry is rejected.
The same tilt limitation on the browser applies to these queries.

## Read the visible area

<Api of="MapState.viewport" /> reports the current rendered size,
visible bounds, and visible region. It is null until the map renders its
first viewport. A composition that reads it recomposes when the camera moves or
the map resizes:

<Code code={region(camera, "viewport")} lang="kotlin" title="App.kt" />

## Convert between screen and geographic coordinates

<Api of="MapState.screenLocationFromPosition" /> converts a geographic
position to an offset from the top-left corner of the map composable.
<Api of="MapState.positionFromScreenLocation" /> converts in the other
direction:

<Code code={region(camera, "convert")} lang="kotlin" title="App.kt" />

## The repeated world and the antimeridian

MapLibre repeats the world horizontally. Geographic values read from the map,
such as <Api of="Viewport.visibleBounds" /> and
<Api of="MapState.positionFromScreenLocation" />, preserve the world copy:
longitudes may extend past ±180°, and the visible bounds may span more than
360°. `VisibleBounds.toBoundingBox()` converts to a GeoJSON `BoundingBox`, where an
antimeridian crossing follows RFC 7946 with an east longitude less than the
west.
