---
title: Expressions in Kotlin
description: The expression DSL that computes layer properties from feature data and zoom.
---

import { Code } from "@astrojs/starlight/components";
import Api from "../../components/Api.astro";
import { region, rememberedMapStyle } from "../../snippets";

import expressions from "../../../../demo-app/common/src/commonMain/kotlin/org/maplibre/compose/docsnippets/Expressions.kt?raw";

Layer properties in MapLibre are not plain values. Each property takes an
expression: a formula that the map evaluates while it renders, per feature and
per zoom level. The [MapLibre Style Specification][spec-expressions] defines
expressions as JSON arrays. MapLibre Compose provides a Kotlin DSL that builds
them with type checking, in <Api of="org.maplibre.compose.expressions.dsl" />.

The DSL uses Compose types when a Compose type matches the property: `Dp` for
sizes, `Color` for colors, and `DpOffset` for offsets.

## Constants

A constant value must also be an expression. Wrap a plain value in
<Api of="const" />:

<Code code={rememberedMapStyle(region(expressions, "constants"))} lang="kotlin" title="App.kt" />

## Read feature data

<Api of="feature" /> accesses the properties of the feature being rendered.
The result is untyped, so convert it with functions such as `asNumber()` or
`asString()`. Functions such as <Api of="step" /> and <Api of="switch" />
select an output from the value:

<Code code={rememberedMapStyle(region(expressions, "feature-data"))} lang="kotlin" title="App.kt" />

The map evaluates this formula once per feature: each earthquake receives a
radius and a color computed from its own magnitude.

## Build arrays from expressions

Use <Api of="semiliteral" /> when each array element needs to evaluate an
expression. `const(listOf(...))` keeps its elements literal. Nested constant
arrays stay literal inside `semiliteral`; nest another `semiliteral` to
calculate their elements too.

<Api of="textVariableAnchorOffset" /> pairs anchors with <Api of="textOffset" />
expressions. EM offsets scale with the label's text size, SP offsets scale with
accessibility font size, and DP offsets keep a fixed distance.

<Code code={rememberedMapStyle(region(expressions, "array-expressions"))} lang="kotlin" title="App.kt" />

Replace old pairs such as `SymbolAnchor.Top to Offset(0f, 1f)` with
`SymbolAnchor.Top to textOffset(0.em, 1.em)`. When `textSize` uses zoom
interpolation, use EM offsets: the engines do not allow the nested zoom
expression needed for SP or DP conversion.

## React to zoom

<Api of="zoom" /> is the current zoom level. <Api of="interpolate" /> computes
a smooth value between stops:

<Code code={rememberedMapStyle(region(expressions, "zoom"))} lang="kotlin" title="App.kt" />

## Filter features

The `filter` parameter takes a boolean expression. The layer renders only the
features for which it is true:

<Code code={rememberedMapStyle(region(expressions, "filter"))} lang="kotlin" title="App.kt" />

## Missing data

Feature data can be incomplete. Use <Api of="coalesce" /> or an assertion's
fallback argument, such as `asNumber(const(0f))`, to choose what to show when a
value is missing:

<Code code={rememberedMapStyle(region(expressions, "missing-data"))} lang="kotlin" title="App.kt" />

Use <Api of="nil" /> to compare an expression with null. To leave a layer
property unset, pass Kotlin `null` to the layer. The API reference describes
nullable expressions and type assertions in detail.

## Where the code runs

Kotlin code outside an expression runs during composition and produces an
expression. The expression runs inside the map renderer, once per feature. A
Kotlin `if` selects which expression to build; an expression `switch` selects
a value per feature. Use Kotlin for decisions that depend on app state, and
expressions for decisions that depend on feature data or zoom.

The API reference documents the full set of expression functions under
<Api of="org.maplibre.compose.expressions.dsl" />.

[spec-expressions]: https://maplibre.org/maplibre-style-spec/expressions/
