# 01: Document the test layers

**What to build:** Publish the layer table and placement rules in the ADR, the
overhaul spec, `AGENTS.md`, and `CONTRIBUTING.md` so new tests land on the
cheapest seam that can catch the bug.

**Blocked by:** None

**Type:** task

**Status:** resolved

- [x] ADR 0001 states the six layers and the placement rules.
- [x] The overhaul spec records the CI diagnosis and the target topology.
- [x] `AGENTS.md` tells an agent where a new test belongs.
- [x] `CONTRIBUTING.md` states the same table for human contributors.

## Answer

The documents landed with the first-wave overhaul. Later tickets implement the
remaining moves and the desktop matrix cut.
